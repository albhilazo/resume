{
  "name": "resume",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "browser-sync start --server --files 'index.html, resume.css, assets/**/*.css'",
    "pdf": "node scripts/pdf.mjs"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
